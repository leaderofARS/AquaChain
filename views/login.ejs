<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>AQUA CHAIN Login</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css">

  <style>
    @import url('https://fonts.googleapis.com/css2?family=Dancing+Script&family=Poppins:wght@400;600&family=Inter:wght@400;500&display=swap');

    body {
      font-family: 'Inter', sans-serif;
    }
    .tagline {
      font-family: 'Dancing Script', cursive;
      font-weight: 400;
      color: #0f172a; /* Deep navy */
      font-size: 1.2rem;
    }
  </style>
</head>
<body class="bg-gradient-to-r from-cyan-50 to-blue-50 h-screen flex flex-col">

  <!-- Navbar -->
  <nav class="fixed top-0 left-0 w-full bg-white shadow-md z-30">
    <div class="max-w-7xl mx-auto px-4 py-2 flex items-center justify-between">
      <div class="flex items-center space-x-2">
        <img src="/aquachain-logo.svg" alt="AQUACHAIN Logo" class="h-10 w-10 object-contain">
        <div>
          <span class="text-blue-900 text-2xl font-bold tracking-wide">AQUA CHAIN</span>
          <p class="text-gray-600 text-sm italic -mt-1">Smart Irrigation Project</p>
        </div>
      </div>
    </div>
  </nav>

  <!-- Main Container -->
  <div class="flex flex-1 items-center justify-center pt-20 pb-10">
    <div class="max-w-5xl w-full bg-white shadow-2xl rounded-2xl overflow-hidden flex h-[550px]">

      <!-- Left Side Image -->
      <div class="hidden md:flex md:w-[55%] bg-gradient-to-b from-blue-200 to-cyan-100 items-center justify-center">
        <img src="../public/ricefield.png" alt="Ricefield" class="object-contain w-full h-full p-6">
      </div>

      <!-- Right Side Login Form -->
      <div class="w-full md:w-[45%] p-10 pt-16 flex flex-col justify-center">
        
        <!-- Heading -->
        <h2 class="text-3xl font-bold text-blue-900 mb-3 text-center font-[Poppins]">Welcome back</h2>
        <p class="text-gray-600 text-center mb-6 text-sm leading-relaxed font-[Inter]">
          Sign in to track and manage your irrigation system
        </p>

        <!-- Error Message -->
        <% if (error) { %>
          <div class="bg-red-100 text-red-700 px-4 py-2 mb-4 rounded-lg text-center text-sm flex items-center justify-center space-x-2">
            <i class="bi bi-exclamation-triangle-fill"></i>
            <span><%= error %></span>
          </div>
        <% } %>

        <!-- Login Form -->
        <form action="/login" method="POST" class="space-y-5">
          <div>
            <label for="username" class="block text-sm font-[Poppins] text-gray-700 mb-1">Username</label>
            <input type="text" id="username" name="username" placeholder="Enter your username" class="w-full px-3 py-2 border border-gray-400 rounded-xl shadow-sm focus:ring-2 focus:ring-blue-600 focus:outline-none transition hover:border-blue-600" required>
          </div>
          <div>
            <label for="password" class="block text-sm font-[Poppins] text-gray-700 mb-1">Password</label>
            <input type="password" id="password" name="password" placeholder="Enter your password" class="w-full px-3 py-2 border border-gray-400 rounded-xl shadow-sm focus:ring-2 focus:ring-blue-600 focus:outline-none transition hover:border-blue-600" required>
          </div>
          <button type="submit" class="w-full bg-blue-900 text-white py-2.5 rounded-xl font-semibold text-base transition hover:bg-blue-800 hover:shadow-md hover:scale-[1.02]">Login</button>
        </form>

         <!-- Footer Links -->
        <div class="mt-2 text-center text-xs text-gray-600 space-x-2">
          <a href="#" class="hover:underline">Terms</a> | 
          <a href="#" class="hover:underline">Privacy</a> | 
          <a href="#" class="hover:underline">IT Support</a>
        </div>

        <!-- Features -->
        <div class="mt-4 grid grid-cols-3 gap-6 text-center text-sm font-[Poppins]">
          <div>
            <i class="bi bi-droplet-fill text-blue-700 text-xl"></i>
            <p class="mt-1">Smart</p>
          </div>
          <div>
            <i class="bi bi-lightning-charge-fill text-yellow-500 text-xl"></i>
            <p class="mt-1">Efficient</p>
          </div>
          <div>
            <i class="bi bi-bar-chart-line-fill text-green-600 text-xl"></i>
            <p class="mt-1">Live Data</p>
          </div>
        </div>

        <!-- Tagline -->
        <p class="mt-2 text-center text-gray-700 text-xs font-medium leading-snug">
          “Every drop counts — Aqua Chain helps you save water while boosting yield.”
        </p>

       

      </div>

    </div>
  </div>

</body>
</html>